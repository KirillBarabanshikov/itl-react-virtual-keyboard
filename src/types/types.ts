export type TKeyboardType = 'text' | 'email';

export type TKeyboardData = {
  rus: {
    default: string[][];
    caps: string[][];
  };
  eng: {
    default: string[][];
    caps: string[][];
  };
  num: {
    default: string[][];
    caps: string[][];
  };
};
